package com.campusiq.config;

import lombok.extern.slf4j.Slf4j;
import org.springframework.context.annotation.Configuration;

/** Firebase stub — configure separately if push notifications are needed. */
@Configuration
@Slf4j
public class FirebaseConfig {
    // Firebase integration disabled. Configure pom.xml + firebase-service-account.json to enable.
}
